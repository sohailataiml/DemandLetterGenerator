# Core service
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0
pydantic>=2.9
python-multipart>=0.0.9
python-docx>=1.1

# Optional, feature-gated
pypdf>=5.0          # PDF text extraction; without it PDFs are stored as NEEDS_OCR
anthropic>=0.40     # only needed when DLG_LLM_PROVIDER=anthropic
alembic>=1.13       # migrations for non-SQLite deployments

# Development
pytest>=8.0
httpx>=0.27

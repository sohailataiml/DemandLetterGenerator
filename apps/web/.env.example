# Copy to .env.local. These are development defaults, not secrets.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# The backend currently authenticates with these two headers. This is a
# development stand-in for a real session; see the auth note in the root README.
NEXT_PUBLIC_USER_ID=attorney_1
NEXT_PUBLIC_USER_ROLE=attorney
